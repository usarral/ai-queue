# PostgreSQL
POSTGRES_PASSWORD=changeme_db

# Redis
REDIS_PASSWORD=changeme_redis

# API
API_KEY=changeme_api_key

# WoL — dirección MAC y IP del AI server en la LAN
AI_SERVER_MAC=AA:BB:CC:DD:EE:FF
AI_SERVER_IP=192.168.1.100
BROADCAST_IP=192.168.1.255

# Caddy — dominio público (dejar en blanco para usar solo IP/localhost)
DOMAIN=ai-queue.example.com
